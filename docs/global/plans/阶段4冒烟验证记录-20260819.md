# 阶段4 冒烟验证记录（2026-08-19）

分支 `refactor/0819-split-generic-pages`，在运行中的 dev 环境（Vite :5173 + API :8000 + MySQL hspsi-dev）完成端到端冒烟验证。

## 前端编译验证（Vite :5173）
- 全部 31 个业务薄壳页 `.vue` 经 Vite 编译均返回 200（无 SFC 编译错误）。
- 引擎 `BusinessDocumentPage.vue`、配置接口、全部复杂表单（SalesPayment/SalesDiscountOrder/InventoryTransfer/InventoryLoss/SalesService/PurchaseRefund 等）编译正常。
- 引擎「新增」按钮修复已生效（`creatable !== false` 渲染 `createText`）。

## API 冒烟验证（登录 admin + test1）
### 列表读取：31 个业务端点全部 200
requisitions(app/output/return)、production(plan/bom/input/output/shortage)、
sales(order/output/return/payment/refund/discount-order/service)、
purchase(application/order/receipt/return/payment/refund)、
inventory(stock/transfer/adjustment/check/loss/loss-output/overflow/overflow-input/quantity-alert/expiry-alert)

### 详情读取（取真实 id）：
requisitions/applications、production/plans、sales/orders、purchase/orders、inventory/transfers 均 200。

### 表单下拉端点（迁移后表单实际调用）：
goods、product-options(production/requisitions)、application-options、output-options、
application-form-options、money-order-options、output-options(sales)、boms、plans、stock-options 均 200。

### 权限模型回归（阶段3 前序成果未破坏）：
- 主数据读取/下拉 = 仅需登录：test1（非 admin）访问 goods/基础资料 options 均 200。
- 金额脱敏：test1 无金额权限，goods.costPrice/salePrice 返回 null。
- 写操作 = 页面+操作 code：test1 有 `purchase:applications` 页面但无 `:create` → POST 403；
  test1 无 `system:roles` 页面权限 → POST 403。
- 业务 GET = 模块 code：test1 有页面权限的 5 类业务 GET 均 200；
  test1 持有 `system:view`（角色2既有配置）故读取 users 200（符合授权，非回归）。
- 行操作端点存在且权限判定正确：真实 id 上 undo-confirm/terminate 返回 400（业务状态不符），
  而非 404/500，说明端点存在、守卫放行、业务校验拦截——符合预期。

### 写链路业务校验正常：
- 领用申请（admin 无 OA 员工）→ 400「未关联OA员工」；
- 盘点创建（admin）→ 400「仓库类型不一致」；
- 采购申请创建（test1 无 create 权限）→ 403。
均为端点正常响应下的业务/权限拦截，非迁移回归。

## 结论
阶段4 全部 31 个业务迁移 + 旧通用页面删除 + 路由/菜单一致 + typecheck/build 通过 +
dev 环境冒烟验证通过。剩余人工浏览器确认项（建议）：
1. `/requisitions/applications` 样板页：新增按钮、创建/编辑表单交互。
2. `/sales/orders` 跨页深链：登记收款/发起退货自动预填订单。
3. `/inventory/checks` 盘点：创建→编辑填数→提交审批。
4. 只读页（`/inventory/stocks`、`/production/shortages`）无新增按钮、行操作正常。
