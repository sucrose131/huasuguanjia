# 当前需求汇总与实施构想

> 文档状态：当前有效实施基线
>
> 初始编制日期：2026-08-07
>
> 最近更新日期：2026-08-09
>
> 配套记录表：`docs/daily/2026-08-09/当前需求实施记录表-20260809.md`
>
> 项目开发规则：`docs/global/rules/AI功能开发与交付规则.md`
>
> 当前前端设计：`docs/global/design/当前需求前端界面设计方案-20260809.md`

## 1. 文档用途和维护规则

本文用于说明当前十五项需求及后续新增集成范围的最终业务口径、实施状态和后续工作。此前讨论稿、历史对话或旧计划与本文冲突时，以本文和配套实施记录表中的最新确认结论为准。

从本次更新开始，每次需求开发必须同时维护两份文档：

1. 本文：更新当前要做什么、业务规则、范围和验收标准；
2. 实施记录表：追加记录实际做了什么、修改范围、验证结果、完成日期和遗留问题。

不能只修改代码而不更新记录表，也不能只在记录表中写“已完成”而不保留验证依据。

所有需求必须先完成界面设计、字段—API—数据库映射和业务确认，再进入正式逻辑开发；具体流程遵守项目级《AI功能开发与交付规则》。

## 2. 当前总体进度

| 状态                           | 数量 | 需求编号               |
| ------------------------------ | ---: | ---------------------- |
| 已完成代码和本轮浏览器复核     |    4 | 8、10、14、15          |
| 已完成代码，自动化测试通过     |    5 | 2、6、7、9、13         |
| 代码完成，仍有专项场景待验收   |    1 | 1                      |
| 代码完成，核心入口浏览器通过   |    1 | 12                     |
| 代码已完成，真实环境交付待完成 |    1 | 3                      |
| 本期暂缓，不作为当前缺口       |    3 | 4、5、11的外部同步部分 |
| 新增集成范围，待设计和实施     |    1 | OA审批集成             |

本轮已完成原有两个核心代码缺口：

1. 商品分类、商品列表和商品详情已按登录账号所属组织拥有的启用仓库类型动态过滤；超级管理员保留全局范围；
2. 采购申请、无来源采购订单和直接采购入库已统一提供“搜索无结果后快捷创建正式商品及默认 SKU”的入口；订单来源入库保持只读。

原十五项需求已进入集中回归。2026-08-09 全量自动化测试、类型检查和生产构建通过；采购快捷建档“保存草稿”500已定位为商品主档代码写入`warehouse_id=null`，但本地真实表该字段仍为`NOT NULL DEFAULT 0`。现已按最小范围将商品基础资料和采购快捷建档统一写入`warehouse_id=0`，未修改数据库表结构；采购申请真实页面保存和数据库关联核对通过。采购单位数字显示和临时出库去向窗口缺口也已在本轮修正并通过浏览器复核。

最新补充的 OA 审批集成不属于原十五项需求的测试缺口，而是新增的正式开发范围。OA 方接口已具备，但华溯管家侧尚未完成接口字段对照、状态回调和执行权限控制，因此整体产品状态应表述为：**原十五项需求正在回归，需求 12 的采购申请真实保存缺陷已修复并通过复测；OA审批集成待设计和开发。**

## 3. 已确认的统一业务规则

### 3.1 商品、分类、组织和仓库

- 商品是全局主档，不直接绑定所属组织、默认供应商或具体仓库；
- 商品分类必须关联一个仓库类型；
- 商品资料及采购快捷建档的分类下拉统一使用树形结构，允许选择任意启用且绑定有效仓库类型的分类，不再限制为叶级分类；
- 分类不直接绑定组织，不建立“分类—组织”关联表；
- 普通组织账号可见的分类和商品，由该组织拥有的启用仓库类型动态推导；
- 组织新增某种类型的启用仓库后，应自动获得该仓库类型对应分类及商品的可见范围；
- 组织停用或删除某类最后一个仓库后，应失去该仓库类型对应分类及商品的业务可选范围；
- 业务单据选择的仓库必须属于当前组织，且仓库类型必须与商品分类要求一致；
- 商品基础资料的可见性过滤和业务保存时的后端校验必须同时存在，不能只依赖前端隐藏。

已确认超级管理员默认查看全部商品和分类；普通账号使用自身登录组织范围。本轮不新增跨组织切换器。

### 3.2 快捷创建商品

- 系统不存在“临时商品”业务类型；
- 单据内快捷创建的商品保存后就是正式商品；
- “商品是否已经存在”必须按全局商品主档判断，不能只查询当前组织可见范围；
- “商品是否允许当前账号使用”按登录组织的启用仓库类型判断，这两个判断不能混为一体；
- 用户优先从现有商品中选择；采购商品下拉使用后端远程关键词搜索，不受页面首次加载条数限制；商品不存在时，可以在业务单据中快捷创建；
- 当前组织范围内搜索不到时，系统必须继续检查全局同名商品：全局已有则禁止重复新增；
- 全局已有且当前组织可用时，直接选择已有商品；全局已有但组织没有对应仓库类型时，提示所需分类和仓库类型，并要求先配置仓库或联系管理员；
- 全局商品已停用、已删除或所属分类不可用时，同样禁止重复新增，并提示联系商品管理员处理原档案；
- 快捷创建输入商品名称、任意启用且绑定有效仓库类型的现有分类和基础单位；
- 系统自动创建正式商品和一条默认 SKU，并回填当前单据明细；
- 商品分类要求的仓库类型必须与当前组织所选仓库匹配；
- 商品、SKU和业务单据必须在同一事务中保存，任一环节失败时整体回滚；
- 商品名称重复、同商品下 SKU 规格重复时禁止重复创建。

系统自动生成“默认规格”SKU，换算数量为 1；供应方式、商品形态和价格沿用既有后端默认值，不在快捷窗口中展示。

### 3.3 库位

- 库位继续作为业务单据中的自由文本字段；
- 库位不作为实时库存结存维度；
- 本期不建立库位历史表；
- 本期不开发个人库位推荐和历史摆放查询。

### 3.4 售后记录和客服进展

- “发货客服跟进”和“售后处理进展”是同一个需求；
- 用户在本系统创建一条售后主记录，并关联销售订单及相关商品；
- 一条售后主记录下面可以增加多条独立客服处理进展；
- 每条进展记录处理内容、状态、处理人和实际处理时间，作为后续操作证据；
- 新增进展不能覆盖售后主记录或历史进展；
- 编辑和软删除进展必须保留操作审计；
- 华数之家售后同步本期暂缓，当前只要求本系统内部售后功能可用。

### 3.5 菜单主列表 ID

- 每个菜单进入后的顶层主列表统一显示“序号 → ID → 原有业务列”；
- ID 是当前主记录真实数据库主键，并按字符串返回，避免 BigInt 精度丢失；
- Dashboard、汇总报表、弹窗明细、SKU 行、BOM 明细、展开子表和无单一主键的聚合表不在本需求范围；
- 当前代码已在通用基础资料、商品、分类、属性、采购、库存、生产、销售、领用和系统主列表中增加序号与 ID，仍需在实际浏览器中按菜单清单做最终复核。

### 3.6 OA审批集成

- 业务表单继续在华溯管家中创建和保存，华溯管家是业务数据主系统；
- 业务环节是否需要OA审批必须形成明确清单，不需要审批的环节继续使用现有流程；
- 需要审批的表单在本系统提交后，将约定的必要字段发送到OA，并进入“审批中”；
- OA是审批结果的唯一判定方，本系统不再执行一套重复的人工审批；
- OA只向本系统回传审批通过或不通过，并可同时回传OA流程编号、审批意见和审批时间；
- 本系统负责验证回调来源、幂等接收结果、更新本地表单审批状态并保留记录；
- 审批通过后，只有表单中指定的执行人或有明确授权的人员可以继续办理后续业务；
- 审批不通过时，表单不得继续执行业务，并应展示OA返回的结果和意见；
- 提供给OA的内容应为稳定的接口字段协议，不直接暴露数据库整表结构；
- 需要统一解决华溯管家账号与OA账号的人员标识映射。
- 普通销售订单来源于已经在线上完成的订单，不需要本系统或OA再次审批；保存后直接进入可办理状态；
- 折价销售单属于独立处置业务，不随普通销售订单取消审批，并已确认需要进入OA审批。
- 采购退货必须进入OA审批；提交时不扣库存，只有OA审批通过后才执行唯一一次库存扣减并生成退款任务；
- 生产缺料清单不需要审批；一旦产生缺料，系统立即自动生成待审批采购申请，所有缺料采购统一在采购申请环节审批。

已形成 `docs/global/integrations/OA审批流程清单与接口字段说明-20260809.md` v0.2 现有字段清单，完成 11 类审批表单、真实主表/明细表和可提供字段的核对，折价销售单已经确认需要进入 OA 审批。本文不替 OA 拟定接口协议；OA 方接口已经完成，待取得 OA 的实际接口说明后，再将我方现有字段与 OA 已有字段逐项对照。

### 3.7 采购单据“更多”与操作记录

- 采购申请、采购订单、采购入库、采购退货、采购付款和采购退款的“更多”不得出现空菜单；
- 不论单据处于草稿、处理中、已完成或已关闭状态，“更多”都必须显示“操作记录”；
- “操作记录”展示当前单据的创建、提交、审批、入库确认、付款、退款及生成下游单据等可由现有业务数据准确还原的动作，并显示操作人、时间、结果和说明；
- “业务链路”属于内部追溯能力，本期不在采购管理菜单中向用户展示；
- 历史数据未单独保存某一步骤时间时，界面必须明确标注“历史表未单独保存”，不得使用其他更新时间冒充；
- 已完成单据继续隐藏已经失效的编辑、提交、审批和删除动作，但保留仍合法的后续动作，例如已入库单发起退货、付款记录撤销等。

本能力复用现有采购主表、明细表、审批字段、创建/更新时间和用户表，不新增数据库表、字段或数据字典。前端通过 `GET /purchase/:resource/:id/operation-history` 读取操作记录。

## 4. 十五项需求当前结论

### 需求一：商品改为全局主档

状态：**本轮代码完成，大部分普通组织账号专项场景已通过。**

已完成：

- 商品新增、编辑和列表不再要求组织、默认供应商和具体仓库；
- 采购及库存过账已具备部分仓库归属和仓库类型校验；
- 商品旧字段暂时停止使用，尚未物理删除。

本轮完成：

- 商品分类接口从登录上下文读取组织，按该组织启用且未删除仓库的仓库类型过滤；
- 商品列表、汇总数量和商品详情使用同一数据范围，越权详情按不存在处理；
- 商品新增/编辑时校验所选分类是否属于普通账号当前组织的可用仓库类型；
- 超级管理员继续使用全局商品范围；
- 采购、生产、销售、领用等前端商品选择统一通过受控 `/goods` 接口取得候选商品。

专项验收结果：使用临时普通账号实际验证了无仓库组织返回空分类/商品、单仓库类型只返回对应类型、多仓库类型只返回允许类型，以及“全局已有但当前组织仓库类型不可用”返回 `warehouse_type_unavailable`；临时账号和角色已清理。停用最后一个同类型仓库仍缺少可直接使用的真实数据夹具，当前仅由状态过滤代码和自动化测试覆盖，后续补一笔真实数据验收。

旧字段物理删除属于稳定运行后的后续工作，不是当前开发阻塞项。

### 需求二：新增商品自动生成默认 SKU

状态：**已完成代码。**

- 新建商品自动准备默认 SKU；
- 未填写 SKU 时自动创建“默认规格”SKU；
- 后端保证默认 SKU 规则；
- 采购单内快捷建档复用相同逻辑。

### 需求三：全业务单据统一附件

状态：**代码基本完成，真实环境交付待完成。**

已完成：

- 各业务主单附件 JSON；
- 阿里云 OSS 私有对象上传、预览、下载和删除；
- 单据状态权限、并发保护和操作日志；
- 扩展名、MIME、大小和数量配置；
- 客户端 IP 审计和孤儿对象扫描/清理脚本。

待完成：

- 配置真实阿里云 OSS；
- 按单据状态验证上传、预览、下载和删除；
- 验证外部定时任务执行孤儿对象扫描；
- 在 dev/production 部署前完成权限和安全复核。

### 需求四：自由文本库位及个人历史推荐

状态：**本期暂缓，不需要完成。**

库位继续允许自由输入，但不记录个人历史、不生成推荐。

### 需求五：库存历史摆放查询

状态：**本期暂缓，不需要完成。**

不建立库位历史表，不开发“某个库位历史上摆放过哪些商品”的查询页面。

### 需求六：入库明细自动生成批号

状态：**已完成代码。**

- 全新入库批号为空时生成上海时区 `PHYYYYMMDD`；
- 销售退货、领用退回、调拨入库和 BOM 退库继承来源批号；
- 已有批号不覆盖，用户可以修改自动生成值。

### 需求七：直接领用出库反向生成申请

状态：**已完成代码。**

- 直接领用出库即时扣库；
- 反向生成已通过申请并建立单据关系；
- 申请被否决时保留已生效出库；
- 幂等生成不操作库存的领用退回草稿；
- 实际退库由操作人员确认完成。

### 需求八：菜单主列表统一增加数据库 ID

状态：**已完成并通过浏览器分类复核。**

顶层菜单主列表按“序号 → ID → 业务列”展示。已通过公司、商品分类、采购申请、库存查询、生产计划、领用申请、售后记录和系统角色/用户等共享页面类型复核；统计报表和内部明细不属于此范围。

### 需求九：报损退货生成采购退货

状态：**已完成代码。**

- 报损去向支持“退货”；
- 同批号多采购来源时由用户选择；
- 报损单不扣库存，采购退货确认时唯一扣库；
- 自动生成关系、幂等和异常状态回退已实现。

### 需求十：售后记录下的客服处理进展

状态：**已完成代码及界面验收。**

- 本系统可创建售后主记录并关联销售业务；
- 一条售后可以增加多条客服进展；
- 支持进展编辑、软删除、状态同步、时间线和操作审计。
- 浏览器已确认售后编辑页存在独立“售后处理进展”编辑器、状态和实际处理时间字段，历史进展使用时间线展示；本地现有唯一售后记录暂无历史进展，因此未写入测试业务数据。

### 需求十一：华数之家售后同步

状态：**外部对接暂缓，当前不需要完成。**

代码中已有外部来源、原始快照、Webhook 和幂等能力，但本期不要求与华数之家真实环境打通。当前验收只覆盖本系统人工创建售后和多条进展。

### 需求十二：采购业务快捷创建正式商品并进入库存

状态：**代码完成；采购申请、无来源采购订单和直接采购入库自动化真实链路通过。**

业务口径已由“临时采购商品”修正为“快捷创建正式商品”。

已完成：

- 采购申请内快捷创建正式商品/SKU；
- 采购订单内快捷创建正式商品/SKU；
- 默认 SKU、重复校验和事务保存。

本轮完成：

- 采购申请、无来源采购订单和直接采购入库统一使用搜索无结果后的快捷新增入口；
- 搜索无结果后的新增入口会先查询全局商品主档，只有全局确实不存在同名商品时才允许打开快捷窗口；
- 全局已有且当前账号可用时自动选择原商品；已有但不可用时显示原因且禁止重复新增；
- 快捷窗口只要求商品名称、任意启用且绑定有效仓库类型的现有分类和基础单位，分类以树形层级展示；
- 页面暂存后显示“待创建”，系统固定准备“默认规格”SKU；
- 直接采购入库保存链路已接入商品、SKU和反向采购单据的同一数据库事务；
- 自动化全链路测试已确认新商品和默认 SKU 的真实 ID 会同时写入反向采购申请明细、采购订单明细和采购入库明细；
- 确认入库测试已确认调用库存过账、递增过账版本并将入库单更新为已确认；
- 关闭或取消父表单不创建商品；浏览器已用唯一测试名称验证取消后商品资料中无记录；
- 订单来源的普通采购入库继续保持来源商品只读。

2026-08-09 首次真实页面验收发现采购申请快捷建档“保存草稿”返回500，失败事务未写入商品和单据。根因是代码按Prisma模型写入`warehouse_id=null`，而本地真实表`hspsi_goods_info.warehouse_id`仍是`NOT NULL DEFAULT 0`。现已将商品基础资料和采购快捷建档两处统一改为`warehouse_id=0`，继续表达“不绑定具体仓库”，没有修改数据库表或字段。

### 采购申请拆单生成采购订单（2026-08-13）

- 采购申请审核通过只完成审批，不再自动生成采购订单；生产缺料来源同样由采购人员人工生成。
- 审核通过后的“更多”提供“整单生成”“选品生成”“关联订单”。
- 整单生成把全部尚未生成明细合并为一张指定供应商订单；选品生成把本次勾选明细合并为一张订单，一张申请允许形成多张订单。
- 同一采购申请明细在有效采购订单中只能出现一次；后端事务校验与数据库唯一索引共同防止重复和并发重复提交。
- 用户录入明细采购总金额，系统按`总金额 ÷ 申请数量`反算两位采购单价；总金额为最终金额口径。
- 可删除的采购订单删除后释放来源明细，允许重新生成；不满足既有删除条件的订单不释放。
- 本功能复用现有采购主表、明细表和数据字典，本轮未新增或修改数据库结构、数据字典。

修复后使用“最小修复商品-20260809-1458”复测成功：创建全局商品ID `1900700624`、默认SKU ID `1900701676`和采购申请 `PA202608090001`，申请明细正确关联商品与SKU；相关API类型检查及34项商品/采购测试通过。按当前最小测试范围，无来源采购订单和直接采购入库本轮不继续做真实页面复测，保留既有自动化链路验证结果。

随后通过真实 API 自动化补测无来源采购订单和直接采购入库。无来源采购订单 `PO202608090001` 已成功创建正式商品、默认 SKU，并在开始采购时反向生成已通过采购申请 `PA202608090002`。直接采购入库 `GA202608090001` 保存成功，但首次确认入库发现库存过账仍按旧模型要求 `hspsi_goods_info.org_id` 等于业务组织，导致全局商品主档 `org_id=0` 被错误拒绝。现已将库存主档校验调整为只接受“全局商品 `org_id=0` 或当前组织历史商品”，继续拒绝其他组织商品；没有修改数据库结构、字典或现有商品数据。使用同一张入库单复测通过：确认状态由待确认更新为已确认，库存由 `0` 增加为 `3`，批号 `PH20260809`、计量单位 `PCS`和“华溯全链路原料仓”均正确。

### 需求十三：BOM 整单出库支持多次退库

状态：**已完成代码。**

- 支持部分、多次及临时补料退库；
- 草稿占用可退数量，作废释放；
- 固定原仓库并继承原批号；
- 完成时唯一入库过账；
- 并发超退拦截和已完成退库冲正已实现。

### 需求十四：临时出库去向增加“其他”

状态：**已完成代码。**

已增加独立 `destination_type` 字段和“实验出库、其他”专用字典，不与普通生产出库类型混用。2026-08-09 回归发现实际使用的临时出库窗口仍写死“实验出库”且未提交该字段；现已改为读取专用字典、显示“实验出库/其他”下拉选项并提交 `destinationType`，浏览器复核通过。

### 需求十五：库存界面增加组织、部门和领用人查询

状态：**已完成代码。**

库存菜单已有领用记录视图，支持组织、部门、领用人、商品、SKU、批号、日期和持有状态组合查询。2026-08-09 已在浏览器选择真实组织并确认全部筛选项、序号/ID列和领用明细表结构正常加载。

## 4.1 新增集成范围：OA审批

状态：**审批清单和我方现有字段核对已完成，待取得 OA 已有接口说明并进行字段对照；不属于原十五项需求的测试范围。**

当前已确认：

- OA方审批接口已经具备；
- 本系统创建并保存业务表单，需要OA审批时才向OA发起流程；
- OA负责审批，本系统只接收可信的审批结果；
- 通过后由表单指定人员继续办理，不通过则禁止继续；
- 本系统必须保存OA流程标识、审批结果、审批意见和时间，并保证回调幂等。
- 普通销售订单明确不进入OA审批，保存后直接进入后续出库、收款、退货、售后或缺口分析流程。
- 采购退货明确进入OA审批，退货申请提交后等待OA结果，审批通过前不得扣减库存；
- 生产缺料清单明确不进入OA审批，也不保留人工“审核后生成采购申请”步骤；缺料产生时直接生成采购申请。
- 已盘点并确认当前 11 类 OA 审批入口：采购申请、采购退货、库存调拨、库存调整、库存盘点、库存报损、盘亏出库、盘盈入库、生产计划、领用申请和折价销售单；
- 已形成 `docs/global/integrations/OA审批流程清单与接口字段说明-20260809.md` v0.2，仅列我方真实表名、真实字段和可关联的展示名称来源，可直接交 OA 开发人员做字段对照。

接口开发前仍需完成：

1. 由 OA 开发人员提供其已经实现的接口说明、字段名称、必填规则、认证方式和测试环境；
2. 将 OA 已有字段与我方现有表单字段逐项对照，不自行新增推测协议；
3. 由双方核实本系统用户、人员、组织、部门与 OA 标识的实际对应方式；
4. 根据双方最终对照结果，再评估华溯管家侧是否需要代码或数据库变更；
5. 完成接口调用和联调测试。

## 4.2 采购管理业务单号

状态：**已完成代码、本地数据迁移和页面验收；不属于原十五项需求。**

统一规则如下：

- 采购单据当前代码和历史数据使用的英文前缀保持原样，不在本次调整中改名；
- 只将英文前缀后面的数字调整为“上海时区日期 `YYYYMMDD` + 当日四位流水号 `NNNN`”；
- 各采购单据类型独立计数，每天从 `0001` 开始，单类型每日上限为 `9999`；
- 页面必须展示数据库业务单号字段，不得再使用数据库 ID 临时拼接单号；
- 数据库主键、上下游外键、表结构和字段均不改变；
- 历史采购申请、订单、入库、退货、付款、退款和退款流水按创建时间稳定重排数字后缀，原英文前缀原样保留。

2026-08-09 已在本地 `hspsi-dev` 执行数据迁移。7 类采购业务表均无格式错误、重复业务单号和临时编号残留；浏览器已确认采购申请、订单、入库、退货、付款和退款页面展示正式业务单号，例如 `PA202608070007`、`PO202608070007`、`GA202608070003`。

## 5. 下一阶段实施范围

前端界面设计已形成，当前处于业务确认阶段。正式实现必须遵守 `docs/global/design/当前需求前端界面设计方案-20260809.md`，表格样式只能从 `docs/global/standards/Element Plus Table表格样式归档.md` 选择。

### 5.1 第一优先级：组织商品可见范围专项验收

当前代码已实现并完成无仓库、单仓库类型、多仓库类型及全局已有但组织不可用场景；剩余专项验收目标：

1. 准备一个仅有停用仓库的可回滚真实数据夹具；
2. 验证停用最后一个同类型仓库后分类和商品立即不可用；
3. 继续保留超级管理员和跨组织账号的授权例外能力回归。

### 5.2 第二优先级：采购入库快捷建档专项验收

当前真实保存测试未通过，下一步按以下顺序处理：

1. 读取后端真实异常栈，定位采购申请保存 500 的数据库或事务原因；
2. 修复后复测采购申请草稿成功保存；
3. 复测无来源采购订单和直接采购入库成功保存；
4. 核对正式商品、默认 SKU、各单据明细和库存过账的真实 ID 关系；
5. 复测重复名称、仓库类型不匹配、事务回滚、重试和并发提交。

### 5.3 OA审批集成设计与实施

1. 盘点需要OA审批的单据、提交节点和审批通过后的执行动作；
2. 向OA开发人员提供公共字段和各单据扩展字段清单；
3. 明确发起审批、查询或补偿、结果回调的API协议；
4. 完成本地审批状态、OA流程标识、回调日志和幂等方案；
5. 完成审批状态界面设计及字段—API—数据库映射；
6. 业务确认后再进入正式开发和联调。

### 5.4 环境与验收

1. 配置真实 OSS 并完成附件状态权限验收；
2. 浏览器逐菜单复核顶层列表序号和真实数据库 ID；
3. 验收本系统售后主记录下多条客服进展；
4. 回归报损退货、直接领用反向申请和 BOM 多次退库；
5. 华数之家同步、库位历史和个人推荐继续保持暂缓。
6. OA审批集成在接口设计和开发完成后单独制定联调、异常补偿和回归测试清单。
7. 修复采购快捷建档真实保存 500 后重新执行完整成功链路。

本地未配置 `OSS_REGION`、`OSS_BUCKET`、`OSS_ACCESS_KEY_ID` 和 `OSS_ACCESS_KEY_SECRET`，因此真实 OSS 上传、预览和下载验收仍属于外部配置待办，不能在当前环境标记完成。

## 6. 本轮已采用的确认结论

1. 超级管理员默认查看全部组织的商品和分类；
2. 普通账号按自身登录组织过滤，普通账号没有任何启用仓库时商品和分类范围为空；
3. 本轮不新增跨组织切换器；
4. 快捷建档固定为商品名称、分类、基础单位三个必填项；分类使用树形下拉，可选择任意启用且绑定有效仓库类型的层级，不限制叶级；
5. 默认 SKU 使用“默认规格”、`pcs_qty=1`、`is_default=1`，价格为 0，供应方式和商品形态沿用既有系统默认值；
6. 采购订单来源入库保持商品只读，直接采购入库开放新商品快捷建档；
7. 保存采购申请草稿成功时也创建正式商品和默认 SKU，关闭或取消未保存表单不创建。
8. 商品名称唯一性按全局主档判断，组织仓库类型只决定可用性，不改变全局唯一规则。
9. 原十五项需求完成测试与OA新增集成范围分开管理，OA未开发不能被记录为原十五项的测试失败。
10. OA负责作出审批决定，本系统负责发起、接收可信结果、更新状态和控制后续执行权限。
11. 采购管理所有计量单位只向用户显示基础资料中的单位文字，`unitType` 数字仅用于接口和数据库传值。
12. 采购管理业务单号保留既有英文前缀，只调整数字后缀为 `YYYYMMDD + 4位日流水`；数据库 ID 继续单独展示，不作为业务单号。

## 7. 当前验收边界

本轮只有同时满足以下条件才能标记为完成：

- 前后端功能均已实现；
- 数据库迁移已经在目标环境执行；
- 类型检查和相关自动化测试通过；
- 关键业务通过浏览器实际操作验证；
- 实施记录表已经追加完成记录；
- 暂缓功能没有被误计为当前缺口或已完成内容。

OA审批集成只有在审批单据清单、字段协议、账号映射、状态规则、界面、接口、回调幂等及联调验证全部完成后，才能标记为“已完成”；当前保持“待设计和实施”。
