# OA审批表单字段映射实施记录（2026-08-12）

## 一、需求

实时拉取华溯集团 OA 已发布的“华溯管家”审批表单，并参照领用申请建立 OA 控件、本地 API 与数据库字段映射。

## 二、本次完成

- 切回 `codex/oa-requisition-approval` 功能分支；
- 只读拉取账套 `account_set_id=1` 的 11 张已发布表单；
- 取得每张表单最新 `formKey`、`formId`、控件类型、`uniqueName`、父子关系和必填配置；
- 新建《OA审批表单实际字段映射-20260812》，完成采购申请、采购退货、库存调拨、库存调整、库存盘点、库存报损、盘亏出库、盘盈入库、生产计划、领用申请和折价销售单的实际字段映射；
- 再次拉取 OA 最新发布版本，确认采购申请已删除“预计总金额”和“参考单价”；
- 确认盘亏类型、盘盈类型、生产计划 BOM 已改为单行文本，领用人已改为人员选择控件；
- 第三次拉取确认库存调拨已发布新版本，发货人、接收人均已改为人员选择控件，并更新对应 `formId`、`uniqueName`；
- 建立 11 张 OA 表单的统一代码映射注册表，集中维护业务类型、`formKey`、已核对 `formId`、控件类型、`uniqueName` 和子元素标识；
- 领用申请改用最新领用类型和领用人控件，图片、签名及普通文件统一上传至 OA 附件控件；
- 确认并实现领用申请保留所属组织、领用部门，分别按本地组织和部门名称传入 OA；
- 完成采购申请 OA 提交接入：组装表头、商品明细、提交人 OA 身份和附件，建立审批实例并防止重复发起；
- 扩展无需鉴权的 OA 流程结束回调，根据审批实例业务类型分发至领用申请或采购申请；
- 完成采购申请回调处理：审批通过自动生成采购订单，未通过执行原有回退逻辑，重复回调幂等处理；
- OA 审批中的采购申请禁止在进销存内重复人工审批；
- 抽取剩余表单共用的 OA 提交器和提交人身份解析器，统一处理审批实例、附件中转、临时文件清理和失败状态；
- 完成采购退货 OA 提交及回调：审批通过扣减库存并生成退款任务，未通过复用原关联恢复逻辑；
- 完成库存调拨、库存调整、库存盘点、库存报损、盘亏出库、盘盈入库的 OA 提交、附件中转和审批回调；
- 盘点审批通过后自动生成的盘亏出库、盘盈入库继续发起各自 OA 审批，子流程发起失败不回滚已完成的主审批；
- 库存报损审批通过后如生成折价销售单，自动继续发起折价销售 OA 审批；
- 完成生产计划 OA 接入：正常待审批、缺料补齐重新校验和历史计划重启均进入 OA，缺料状态不误发审批；
- 完成折价销售单 OA 提交及回调，普通销售订单继续保持无需 OA 审批；
- 所有已接入单据在 OA 流程活动期间禁止进销存端重复人工审批，回调均记录原始报文并支持幂等；
- 对照数据库 Schema 核对主表、明细表和字段来源；
- 识别领用申请新版表单与现有代码的字段错位风险，暂未继续真实发起；
- 整理 OA 配置修正清单，等待 OA 修改发布后再次拉取确认。

## 三、发现的问题

1. 全部表单控件均为非必填，需要 OA 侧确认必填策略；
2. 尚未进行真实 OA 流程联调，需在 DEV 外网环境逐张表单验证 OA 控件取值和回调结果。

## 四、当前状态

11 张 OA 表单已完成统一代码映射、提交和审批回调接入；当前进入自动化回归与 DEV 环境逐单联调阶段。

## 五、验证结果

- OA 字段注册表测试：4 项通过；
- 领用申请 OA 提交与附件测试：4 项通过；
- 采购申请 OA 映射、发起和原有业务测试：26 项通过；
- OA 回调分发测试：5 项通过；
- API TypeScript 全量类型检查：通过；
- API 单元测试：175 项通过，20 项因需外部服务而跳过；
- API 模块依赖与路由初始化通过；本次沙箱无法连接本机 `127.0.0.1:3306`，因此未继续执行数据库实单测试；
- 未发起真实 OA 审批，避免在正式 OA 产生测试审批单。

## 六、Git

- 分支：`codex/oa-requisition-approval`；
- 提交号：待提交。
