# 工作日志（AI 开发工程师）· 2026-08-21

> 分支：`dev`
> 统计范围：仅 2026-08-21 的 git 提交，不含提交用户名为 `hd` 的提交
> 本版与《工作日志-产品经理视角-20260821.md》逐条对应（业务语言 ↔ 技术语言）

## 今日工作总结

1. **采购流程恢复完整可用**
   - 合并 `review/0820-remote-dev`（PR #18）到 dev：基于远端 dev 的独立分支完成重构前后功能等价性审计（对照旧 `PurchasePage/InventoryPage/WorkflowPage` 与共享引擎迁移后差异、风险分级）及共享引擎扩展与组件边界设计；
   - 恢复采购申请「整单生成 / 选品拆单 / 关联订单」及采购订单行级付款入口：拆单仅允许选择尚未生成的明细，重新接入独立拆单弹框（`PurchaseApplicationPage`、新增 `PurchaseDocumentPage`）；
   - 修复采购订单无法选择所属组织（查询/表单组织下拉）。

2. **单据查看与操作体验修复**
   - 修复列表空筛选参数被误判为状态 0（`goods/inventory/purchase` 服务及共享引擎筛选参数处理），采购申请等列表默认显示全部单据而非仅「待审批」；
   - 列表创建人列通过用户反查显示姓名，不再固定为「—」（`BusinessDocumentPage` + `use-business-document-options`）；
   - 恢复库存盘点同弹框连续录入、生产补料明细（新增 `SupplementHistoryDialog`）、附件、操作日志及全链路追溯组件。

3. **权限与金额管理更规范**
   - 完善 RBAC 细粒度鉴权：共享引擎新增页面新增权限与行操作权限过滤（查看=页面权限、编辑=`update`、删除=`delete`、提交/审核/确认/过账=各自独立操作权限）；修复模块权限替代页面权限、编辑权限覆盖专项操作权限、系统管理接口越权（`permission.guard` / `request-permission.ts` / `system.controller`）；
   - 组件边界收口：权限推导与字典/选项加载、组织联动、追溯状态抽为独立 Composable（`use-business-document-permissions.ts`、`use-business-document-options.ts`），`BusinessDocumentPage.vue` 由 574 行降至 463 行；
   - 重构采购金额模型：录入明细总金额、按数量反算单价（`price` → `totalAmount`/`lineUnitPrice`），接入金额字段白名单控制（`canEditAmount` / `canViewAmount`）。

4. **外部审批与登录稳定**
   - 新增 OA 审批总开关 `XINFUTONG_OA_APPROVAL_ENABLED`，按环境启停 OA 推送（`document-submission` 及采购/领用 OA 服务）；
   - 采购申请 OA 提交人身份改按「(登录用户, 单据账套)」解析并校验 `out_staff_id`，与领用申请多账套身份口径对齐；
   - JWT 瘦身：token 不再携带 267 个权限与授权组织，10683 字符降至约 588 字符，修复 DEV 外网登录 400 Request Header Or Cookie Too Large。

验证：TypeScript 类型检查、生产构建、API 单元测试、多账号权限回归及真实写入链路验证通过；`review/0820-remote-dev` 已合并 `dev`，0821 各提交均在 `dev`，未合并 `main`。

## 明日工作计划

1. 执行采购、库存、领用、生产、销售及售后的端到端业务链路测试，覆盖正向流转与逆向冲销。
2. 专项验证库存事务幂等性、并发重复提交、过账唯一性、事务回滚及多组织数据隔离。
3. 根据测试结果修复缺陷并补充回归用例，继续完成 OA、华溯之家等外部系统的接口联调与异常场景验证。
