# 采购/领用审批状态 approve_status=3 显示补充实施记录

> 日期：2026-09-03（分支 `feature/0902-oa-cancel-master`）
> 类型：展示文案口径修正（业务负责人确认，随采购撤回/终止功能一并落地）


## 一、需求确认（最终口径）

> 注：曾试将兜底「待审批」改为「待提交」（草稿/撤回待重提避免误读为审批中），业务确认后**回退**：兜底仍为「待审批」，仅新增 approve_status=3 分支。


1. 补 `approve_status=3`（取消/终止）显示分支：本系统终止（创建人操作）→「已取消」；OA 侧撤销（回调 0 号操作人）→「OA已取消」。此前 3 会错误落到「待审批」兜底。

## 二、实施内容

| 文件 | 修改 |
| --- | --- |
| `apps/web/src/utils/approval-status.ts` | `approvalStatusText`：新增 `approveStatus===3` 分支（按 `approve_by===0` + `oaStatus==='CANCELED'` 区分 OA 侧撤销/本系统终止）；兜底仍为「待审批」（曾试改「待提交」，业务确认回退）。`approvalStatusType`：取消类 → `info` 灰。 |
| `apps/web/src/utils/approval-status.spec.ts` | 新增 3/已取消/OA已取消 渠道与颜色用例；兜底「待审批」断言不变。 |
| `apps/web/src/views/business/configs/requisition-application.spec.ts` | 补 3 渠道用例（共用同一工具）。 |
| `apps/web/src/views/business/configs/purchase-application.ts` | 仅头注释（无功能改动）。 |

数据库字典未改动：`approval_status` / `requisition_approval_status` 被库存类单据等多处共用，字典名仍为「待审批/已审批/已取消」，采购页筛选下拉与表单详情回退仍显示字典名（如需列/筛选项完全一致，需单独处理采购页筛选选项，另行确认）。

## 三、验证

- Web `vue-tsc` 通过；`vitest` 76/76 通过（新增 2 个用例）。

## 四、Git 提交

见 git log（提交后回填）。

## 五、（补充）OA 审批在途单据隐藏 通过/驳回 按钮

- 背景：采购申请 OA 在途（`oaStatus` ∈ PENDING_PUSH/RUNNING/BACKTOSTART）时后端已拦截系统内审批（`该采购申请正在OA审批，不能在本系统审批`），前端 通过/驳回 按钮无作用且误导。
- 实施：`apps/web/src/views/business/configs/purchase-application.ts` 新增 `hasActiveOaApproval`/`canApproveApplication`（与领用同口径），`approve`/`reject` 的 `show` 改为 `canApproveApplication`——OA 在途不展示，`PUSH_FAILED`/无 OA 实例（OA 停用或历史单据）仍可系统内审批；撤回/终止不受影响。
- 用例：`remaining-p0-actions.spec.ts` 新增覆盖（RUNNING/PENDING_PUSH/BACKTOSTART 隐藏，PUSH_FAILED 显示，撤回/终止仍显示）。
