# OA 测试环境表单切换实施记录（正式表单切至 Test 表单）

日期：2026-09-01
关联：`docs/daily/2026-09-01/OA已配置表单拉取核对记录.md`（本次切换的数据基础）
范围：仅本地/测试环境数据库（hspsi）；不涉及代码提交

## 一、需求与业务确认

- 背景：账套1（华溯集团）、账套2（华溯生物科技）的「华溯管家-采购申请/领用申请」正式表单已被**生产环境**使用；本地测试环境若继续用同一 formKey 提交 OA，测试审批会与生产混在同一表单下。
- 业务确认（2026-09-01）：
  1. 只切「采购申请 + 领用申请」（OA 仅有这两张启用的 Test 表单，账套1/2 都有，控件结构与正式版完全一致，uniqueName 全部相同）；
  2. 账套1 + 账套2 都切；
  3. 本地 3 个在途采购审批实例（旧表单提交）不做特殊处理，直接切换。

## 二、切换内容

| 账套 | 模板ID | 业务类型 | 原（华溯管家-*） | 新（Test-*） |
| --- | --- | --- | --- | --- |
| 1 | 24 | purchase_application | `AAC15400_NFORM_380054577920868353` | `AAC15400_NFORM_383901863593902089` |
| 1 | 23 | requisition_application | `AAC15400_NFORM_380014832305831937` | `AAC15400_NFORM_383900510679728131` |
| 2 | 35 | purchase_application | `AAC22502_NFORM_380419508420083713` | `AAC22502_NFORM_383901620937752633` |
| 2 | 34 | requisition_application | `AAC22502_NFORM_380047654444138497` | `AAC22502_NFORM_383901131311087668` |

- 更新 `hspsi_oa_form_template` 4 行：`form_key`、`form_id`（对应 Test 表单最新版本）、`form_name`、`form_config`（Test 表单完整配置，与正式版控件结构一致）、`synced_at/updated_at`。
- `hspsi_oa_form_field_mapping` 不变（Test 与正式表单 uniqueName 完全一致，已逐控件验证：采购申请 11 控件、领用申请 11 控件均相同）。
- 代码常量 `OA_FORM_MAPPINGS` **不改**（仅作为账套1 DB 缺失时的兜底；若改为 Test 表单会污染生产兜底路径）。

## 三、验证结果

- DB 回查：4 行 form_key/form_id 均为 Test 表单，synced_at 已更新。
- 运行时读取（OaFormMappingService.getMapping 新进程）：4 组合均返回 Test formKey/formId，字段 11 个、FinTable 子控件数 领用 2 / 采购 5，与表单结构一致。
- 回调链路：回调按 (form_key, account_set_id) 查模板分发，Test 表单提交的审批结果可正常回写。
- API dev 服务已重载（touch main.ts 触发 --watch）清空进程内映射缓存；health 200、登录正常。

## 四、遗留与风险（用户已确认接受/需跟进）

1. **3 个在途采购审批实例**（PA202609010001 / PA202609010007 / PA202609010009，旧表单提交，OA 侧 RUNNING）：切换后其审批结果回调因旧 formKey 不再被识别而忽略，单据停留在"OA 审批中"，需人工处理（用户已确认）。
2. **其余 9 种业务表单**（采购退货、库存调拨/调整/盘点/报损、盘亏、盘盈、生产计划、折价销售）仍使用「华溯管家-*」正式表单，测试提交仍进入生产 OA 同名表单；如需完全隔离，需在 OA 创建对应 Test 版本后再切（另行确认）。
3. **兜底常量风险**：账套1 DB 映射若缺失，代码会降级到常量（生产 formKey），测试环境存在误发正式表单的潜在风险；后续可在测试部署中关闭降级或维护独立的测试常量。
4. 临时脚本 `pull-oa-forms.ts` 未提交，如需保留为正式拉取工具另行确认。
