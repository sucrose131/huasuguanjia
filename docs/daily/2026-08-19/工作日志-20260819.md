# 工作日志（AI 开发工程师）· 2026-08-19

## 工作总结

1. 完成业务列表筛选区字段级联动机制改造：共享引擎 `BusinessDocumentPage` 的 `QueryField` 新增 `dependsOn` 与 `loadOptions`，支持字段声明依赖关系（如 仓库 依赖 组织），依赖值变化时自动清空下游字段并重新加载选项、刷新查询；联动过滤全部走后端（`/base-data/{warehouses|departments}/options?orgId=`），不做前端过滤。补齐采购申请及库存 9 个列表页（盘点/调拨/调整/报损/报盈/报损出库/报盈入库/数量预警/效期预警）的「组织→仓库」联动筛选，并为库存 `checks/loss/loss-output/overflow` 列表接口补充 `orgId` 查询参数。提交 `88527ed`。

2. 完成表单「组织→仓库→商品」三级联动统一化：后端新增采购模块 `/purchase/product-options`（复用 `BusinessMasterDataService.goodsOptions`，按单据组织+仓库类型过滤商品），修正领用 `product-options` 改用单据组织 `orgId`（原错误使用登录账号组织）；12 个业务表单（销售订单/折价、采购申请/订单/入库、领用申请/出库、库存报损/报盈/调拨/调整、生产临时出库）对齐统一链路——换组织清空仓库与明细、换仓库清空不匹配明细、商品按 `orgId+warehouseId` 走后端 `product-options/stock-options` 过滤、未选仓库时商品禁用。

3. 完成仓库/部门选项去前端过滤：将 15 个表单的仓库/部门选项从「全量加载 + 前端 `filter(orgId)`」改为「组织变化时走后端加载」，覆盖销售订单/折价/出库/退货/收款/退款、生产 BOM/成品入库/临时出库、库存报损/报盈/盘点/调拨（调出/调入双组织维度，保留「调入排除调出仓库」业务规则）/调整、采购付款（部门+仓库）；明确前端/后端过滤边界，保留商品 keyword 二次搜索、库存批次行内联动、来源单据剩余量过滤、收款/退款部门归属校验等合法前端过滤。提交 `7d16cf8`。

4. 完成领用申请、采购申请、采购订单「先填商品后选兼容仓库」改造：后端新增 `/requisitions/all-goods-options`、`/purchase/all-goods-options`（复用新增 `BusinessMasterDataService.goodsOptionsByOrg`，按单据组织返回全部启用商品并携带分类 `categoryWarehouseType`）；前端商品搜索改按组织（未选仓库也可搜索），明细商品唯一分类类型决定仓库可选范围（`documentWarehouseType` + `warehouseOptions`，对应老页面 `compatibleWarehouses` 逻辑），换商品/换仓库均做类型兼容校验与提示，编辑回显自动补齐行分类类型，采购订单来源申请带入的明细行同步携带类型。提交 `65ca5a4`、`d2ecf69`。

5. 完成 OA 人员同步 out_staff_id / post_id 为空缺陷修复：根因是 `getAllMembers()` 未传 `extFields` 导致 `idRelation.staffId`（→ `out_staff_id`）与 `post`（→ `post_id`）从不返回，全库 260 人 `out_staff_id` 全空、人员控件提交即报「人员尚未关联有效OA账号」；修复为缺省传 `extFields=['org','position']`。验证：三账套同步成功，分别回填 out_staff_id 209/44/7 人，同人多账套身份按 `(outer_ref_id, account_set_id)` 独立成行互不覆盖，三表关联完整性核对通过。提交 `f312c42`。

6. 完成 OA 账套维度表单映射改造（DB 承载）：表单模板与字段映射从硬编码常量改为按 `(business_type, account_set_id)` 从 `hspsi_oa_form_template`（22 行）+ `hspsi_oa_form_field_mapping`（374 行）读取；新增 `OaFormMappingService`（进程内缓存 + `invalidate()`，账套1 无 DB 映射时降级常量并告警）；6 个 `*-oa-approval.service.ts` 切换为提交时按单据账套动态读取。提交 `d5d4542`。

7. 完成领用申请 OA 跨账套错配兜底与多账套身份匹配：后端账套以单据所属组织为准（`org_id → account_set_id`），提交人按 `(user_id, account_set_id)` 经 `hspsi_sys_user_oa_staff` 解析并校验与 `applicant_id` 一致；新增 `resolveApplicantIdentity` 与 `GET /requisitions/current-applicant?orgId=`，多账套用户选不同账套组织时身份/部门/表单/ORGSEQ 全部在该账套内解析，找不到身份可保存草稿、提交被拦截；前端组织下拉=授权组织，选组织自动填领用人（锁定）+主部门。验证：requisition 17/17、OA 相关 63/63，苏碧安账套1/2 组织各解析对应身份。提交 `05e6eb3`、`d8cd8b1`。

8. 修复单元格 tooltip 错位与 RemoteSelect 下拉漂移：引擎默认关闭 `show-overflow-tooltip` 仅 43 个长文本列开启，避免 fixed 列定位偏移；RemoteSelect 补 `teleported` + `popper-options`（`strategy: fixed`）。提交 `2d91350`。

9. 完成验证与环境恢复：API 单元测试 purchase 28 项、requisition 17 项、OA 相关 63 项、inventory 47 项通过；Web/API typecheck 与生产构建通过；API 接口冒烟（product-options / all-goods-options / warehouses options / checks / losses orgId 过滤）正常；排查并恢复 API dev 进程异常退出，确认本地数据库连接 `hspsi-dev`。

## 明日工作计划

1. 浏览器人工回归：逐一验证列表「组织→仓库」联动、表单「先商品后仓库」联动（领用/采购申请/采购订单）、tooltip 悬停、RemoteSelect 下拉定位、领用多账套身份解析等本次改动效果，输出问题清单并修复。
2. 解决 SKU 规格显示回退为数字 ID 问题（16 处 `skuSpec || skuId` 回退逻辑），与业务确认后统一修复，避免表单明细规格展示异常。
3. 评估商品 keyword 二次搜索后端化（`product-options` 增加 keyword 参数），消除剩余前端过滤依赖场景。
4. 推进真实 OA 发起联调（DEV 外网环境，账套1/2 各走一张单据确认 `FinPeopleSelect` 人员控件渲染）；表单配置重新填充后调用 `OaFormMappingService.invalidate()` 清缓存。
5. 建立普通用户、单组织、多组织、财务白名单等代表账号测试矩阵，自动遍历全部菜单、页面与公共接口，形成权限与数据联动回归问题清单并逐项修复。

> 分支：`refactor/0819-split-generic-pages`；相关实施记录见 `docs/daily/2026-08-19/`（OA人员同步out_staff_id修复、OA账套维度表单映射）。
