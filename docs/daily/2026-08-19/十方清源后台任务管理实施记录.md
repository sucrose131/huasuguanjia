# 十方清源后台任务管理实施记录（2026-08-19）

## 一、需求

在系统管理「任务管理」中纳入十方清源用户、商品、订单同步，使管理员能按 Cron 调度或立即执行。沿用现有任务管理页、表和权限，不新增表、字段或字典。

## 二、范围

- **本次**：注册 `shifang-qingyuan:users/goods/orders`，处理器调用既有同步服务，种子默认停用。
- **暂缓**：十方清源手工 HTTP 同步接口、OSS 清理、任务参数入库、多 API 实例分布式锁。

## 三、影响

| 页面/接口/数据 | 变化 |
| --- | --- |
| 系统管理 > 任务管理 | 类型下拉与列表可出现十方清源三类任务 |
| `GET /system/scheduled-task-types` | 新增三个已实现类型 |
| `hspsi_sys_scheduled_task` | 预置三条，默认停用，Cron `0 1 * * *` |

| 任务名称 | 编码 | 调用 |
| --- | --- | --- |
| 十方清源用户同步 | `shifang-qingyuan:users` | `syncUsers({ operatorId: 0n })` |
| 十方清源商品同步 | `shifang-qingyuan:goods` | `syncGoods('0')` |
| 十方清源订单同步 | `shifang-qingyuan:orders` | `syncOrders('0')` |

不改表结构。种子脚本：`database/migrations/20260818_shifang_qingyuan_scheduled_tasks.sql`。

## 四、验证

| 项 | 结果 |
| --- | --- |
| 定时任务单测 `src/scheduled-task` | 48 条通过 |
| 种子回查 `hspsi-dev` | 已写入 `shifang-qingyuan:users/goods/orders`，均停用，Cron `0 1 * * *` |
| 浏览器页面确认 | 待管理员账号在「任务管理」页确认类型下拉与列表 |

## 五、状态

代码完成待验收。启用前需确认十方清源机构映射、商品分类常量和数据源已就绪。未提交 Git。

## 六、主要文件

- `apps/api/src/scheduled-task/scheduled-task.constants.ts`
- `apps/api/src/scheduled-task/scheduled-task.handlers.ts`
- `apps/api/src/scheduled-task/scheduled-task.module.ts`
- `database/migrations/20260818_shifang_qingyuan_scheduled_tasks.sql`
- `docs/global/plans/定时任务管理实施方案.md`
- `docs/用户操作指引/08-报表与系统管理.md`
