# 每日工作记录说明

每日新建一个 `YYYY-MM-DD` 目录，记录当天实际发生的工作，不在这里复制长期有效的全局方案。

每日目录至少包含：

- 当天处理的需求编号或主题；
- 实际完成的功能和影响范围；
- 自动化测试、接口测试、数据库核对或页面验收结果；
- 未完成事项和阻塞原因；
- 当天本地 Git 提交号。

长期业务规则发生变化时，应同时更新 `docs/global` 中对应的全局文档，并在当天记录中说明变化内容。
