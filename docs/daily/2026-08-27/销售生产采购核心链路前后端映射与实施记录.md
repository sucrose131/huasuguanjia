# 销售生产采购核心链路前后端映射与实施记录

日期：2026-08-27

## 一、实施边界

- 当前后端代码、数据库表结构和数据字典冻结；
- 只修改现有前端配置、现有表单组件和前端测试；
- 不新增页面级组件，不重新设计整张页面，不把代码合并回通用大文件；
- 如确需新增组件，先停止开发并提交产品审核。

## 二、首轮映射矩阵

| 业务环节         | 现有后端能力                                                       | 当前前端状态                                                                                | 首轮处理                                                             |
| ---------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| 销售订单缺口分析 | `POST /sales/orders/:id/analyze`，返回生产计划、缺料及采购申请结果 | “缺口分析”按钮和接口调用已存在，但完成后只显示一条通用提示                                  | 在原行操作中补充生产计划、缺料及采购申请数量反馈，不新增弹框         |
| 生产计划查看     | 详情返回来源销售订单、原料仓、成品仓、五类状态、BOM 明细和缺料明细 | 表单存在，但原料仓未展示；状态字段依赖不存在的 `xxxName`，查看时可能显示“—”；缺料明细未展示 | 在现有 `ProductionPlanForm` 内补齐字段映射、字典状态和只读缺料表格   |
| 生产计划重校库存 | `POST /production/plans/:id/recheck`，状态前置条件完整             | “重校库存”按钮及显示条件已经存在                                                            | 保持现有组件，增加前端显示条件测试，不改接口                         |
| 缺料清单         | 列表返回 `planId`、`purchaseId`、需求/库存/缺口/建议采购和状态     | 列表和查看弹框存在，但关联生产计划与采购申请只是编号，没有快捷操作                          | 在原列表行操作中增加“查看生产计划”“查看采购申请”，复用现有页面和弹框 |
| 采购申请         | 详情返回 `sourceType=production_plan`、`sourceId`及缺料明细来源    | 采购申请主流程、生成采购订单功能已存在；列表接口不返回来源字段                              | 不伪造列表来源字段；通过缺料清单跳转详情和全链路追溯完成关联查看     |
| 采购入库         | 后端确认入库后回写缺料和生产计划物料状态                           | 页面和确认入口已存在                                                                        | 后续按真实测试数据验证刷新与状态变化，首轮不改界面                   |
| BOM 出库         | 后端按计划生成并执行批次出库                                       | 生成入口、执行弹框已存在                                                                    | 后续验证状态条件、批次和刷新，不新增组件                             |
| 成品入库         | `POST /production/inputs`                                          | 表单存在                                                                                    | 后续验证计划筛选、累计入库和库存刷新                                 |
| 销售出库         | 创建、确认、撤销及库存过账接口存在                                 | 表单和按钮存在                                                                              | 后续验证生产入库后的成品可选、部分出库及剩余数量                     |

## 三、实施进度

| 阶段 | 状态 | 修改内容 | 测试结果 | 备注 |
| --- | --- | --- | --- | --- |
| 映射矩阵 | 已完成 | 完成现有接口、按钮、表单和字段差异核对 | 代码只读核对 | 当前任务未改后端、数据库及字典 |
| 销售订单结果反馈 | 已完成 | 原“缺口分析”操作补充生产计划、缺料及采购申请数量反馈 | 自动化通过 | 复用现有消息提示；操作后由通用页面自动刷新 |
| 生产计划查看 | 已完成 | 在原表单补齐原料仓、五类字典状态及关联缺料明细 | 自动化通过 | 复用现有表单和 Element Plus 表格 |
| 缺料关联跳转 | 已完成 | 原行操作增加查看关联生产计划、采购申请的路由深链 | 自动化通过 | 复用现有页面和查看弹框 |
| 采购入库至重校库存 | 已完成代码校验 | 采购订单生成入库单、办理入库及重校库存均沿用既有实现；重校按钮条件与后端前置条件对齐 | 自动化通过 | 未改采购入库和库存过账逻辑 |
| 生产出入库 | 已完成代码校验 | 保留原“生成BOM出库”和批次执行弹框；补齐生产计划直达既有成品入库表单入口 | 自动化通过 | 未新增组件，未改生产过账逻辑 |
| 销售出库闭环 | 已完成代码校验 | 保留销售订单“直接出库”、销售出库表单及确认过账操作 | 自动化通过 | 未新增组件，未改销售过账逻辑 |

## 四、自动化验证结果

- 前端完整测试：`14` 个测试文件、`46` 个测试用例全部通过；
- 前端类型检查：通过；
- 代码格式及空白错误检查：通过；
- 已覆盖：缺口分析结果提示、重校库存显示条件、缺料关联跳转、BOM 出库入口、批次执行弹框、成品入库入口、销售出库入口、采购订单生成采购入库单；
- 本次没有新增页面或业务组件，没有把逻辑写回通用大文件。

## 五、真实页面验收状态

本地页面可正常打开，销售订单页面结构可正常渲染。但当前登录账号显示“未配置角色”“所属组织未配置”，且销售订单列表为 `0` 条，暂时不具备完整业务链路的真实数据前置条件。因此：

- 页面可访问性已验证；
- 配置、按钮条件、路由和字段映射已由自动化测试验证；
- 真实有数据的端到端场景尚未执行，不将其标记为通过；
- 后续需要准备一个具备角色、所属组织、BOM、成品/原料仓库及可用库存的本地测试账号和测试数据，再执行计划中的 E2E-01 至 E2E-06。

## 六、本次实际修改范围

本次核心链路恢复只涉及以下前端及文档文件：

- `apps/web/src/views/business/configs/sales-order.ts`
- `apps/web/src/views/business/configs/production-plan.ts`
- `apps/web/src/views/business/configs/production-shortage.ts`
- `apps/web/src/views/business/forms/ProductionPlanForm.vue`
- `apps/web/src/views/business/forms/ProductionInputForm.vue`
- `apps/web/src/views/business/configs/sales-order-actions.spec.ts`
- `apps/web/src/views/business/configs/production-chain-actions.spec.ts`
- 本实施记录

说明：工作区内存在本任务开始前遗留的其他修改，其中包含库存范围相关的后端文件；这些不属于本次核心链路恢复，本次没有继续修改这些后端文件。
