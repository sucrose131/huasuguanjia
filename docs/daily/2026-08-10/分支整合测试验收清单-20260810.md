# 分支整合测试验收清单（2026-08-10）

## 一、整合基线验证

| 验证项                      | 结果                     |
| --------------------------- | ------------------------ |
| Prisma validate             | 通过                     |
| Prisma Client generate      | 通过                     |
| API/Web/Contracts typecheck | 通过                     |
| API安全自动化测试           | 整合阶段153项通过        |
| API当前回归测试             | 154项通过                |
| Web自动化测试               | 当前累计11项通过         |
| API/Web/Contracts build     | 通过                     |
| `pnpm dev`                  | 本机可启动               |
| API health                  | MySQL、Redis均为up       |
| Web HTTP                    | 200                      |
| 真实外部集成测试            | 20项默认跳过，待授权联调 |
| 浏览器视觉验收              | 待补                     |

## 二、商品分类选择改造验证

| 用例                           | 结果                        |
| ------------------------------ | --------------------------- |
| 商品保存允许启用父分类         | API单元测试通过             |
| 采购快捷新增允许启用父分类     | API单元测试通过             |
| 停用分类仍不可保存             | 保留原后端状态校验          |
| 未绑定有效仓库类型仍不可保存   | 保留原后端仓库类型校验      |
| 组织不可用仓库类型仍被拦截     | 保留原后端组织范围校验      |
| API typecheck                  | 通过                        |
| Web typecheck                  | 通过                        |
| API测试                        | 154项通过，20项外部测试跳过 |
| 商品页树形下拉视觉和操作       | 待浏览器验收                |
| 采购快捷建档树形下拉视觉和操作 | 待浏览器验收                |

## 三、采购商品远程搜索验证

| 用例                         | 结果                                 |
| ---------------------------- | ------------------------------------ |
| Web typecheck                | 通过                                 |
| Web生产构建                  | 通过，仅有原有包体积提示             |
| “徐福记米格玛”关键词数据核对 | 返回商品ID `1900700531`，仓库类型为7 |
| 防抖和过期响应保护           | 已实现250ms防抖与请求版本校验        |
| 搜索失败处理                 | 显示失败提示，不误判为可快捷新增     |
| 真实商品下拉操作             | 待浏览器验收                         |

## 四、商品名称唯一性核对

| 核对项                     | 当前结果                   |
| -------------------------- | -------------------------- |
| 商品资料保存前全局同名检查 | 已实现                     |
| 采购快捷新增全局同名检查   | 已实现                     |
| 编辑时排除当前商品         | 已实现                     |
| 停用、软删除同名商品拦截   | 当前查询会拦截             |
| Prisma唯一索引声明         | 已声明 `uk_goods_name`     |
| 实际数据库唯一索引         | 待只读确认                 |
| 商品名称输入阶段即时提示   | 未实现                     |
| Prisma `P2002`友好提示     | Goods/Purchase尚未统一实现 |
| 华数之家同步同名规则       | 尚未统一                   |

## 五、单位名称显示修正验证

| 用例                   | 结果                                     |
| ---------------------- | ---------------------------------------- |
| API已返回 `unitName`   | 优先展示名称                             |
| 只有 `unitType`        | 通过计量单位选项映射名称                 |
| 字符串和数字单位ID     | 均可正确映射                             |
| 无法匹配单位           | 显示“—”，不显示原始ID                    |
| 统一业务单据明细       | 已接入统一映射                           |
| 生产出库查看、编辑弹窗 | 已接入统一映射                           |
| 单位映射自动化测试     | 新增3项，全部通过                        |
| Web当前累计自动化测试  | 单位修正阶段3个测试文件、8项测试全部通过 |
| Web typecheck          | 通过                                     |
| Web生产构建            | 通过，仅有原有包体积提示                 |
| 浏览器页面确认         | 待人工或后续浏览器验收                   |

## 六、部署路由和HTTP请求键兼容验证

| 用例                       | 结果                              |
| -------------------------- | --------------------------------- |
| 测试环境首页直接访问       | HTTP 200                          |
| `/purchase/orders`直接访问 | HTTP 404，确认部署层缺少SPA回退   |
| `/goods/list`直接访问      | HTTP 404，确认部署层缺少SPA回退   |
| HTTP环境缺少 `randomUUID`  | 使用 `getRandomValues`生成UUID v4 |
| 原生 `randomUUID`可用      | 优先使用浏览器原生实现            |
| 无Web Crypto               | 最终兼容回退可生成合法请求键      |
| 前端原生UUID直接调用       | 已全部替换                        |
| 请求键兼容自动化测试       | 新增3项，全部通过                 |
| Web当前累计自动化测试      | 4个测试文件、11项测试全部通过     |
| Web typecheck              | 通过                              |
| Web生产构建                | 通过，仅有原有包体积提示          |

## 七、待处理环境和交付事项

- 本地数据库部分采购主表缺少 `attachments` 字段，Dashboard查询会报Prisma字段不存在；
- 现有附件迁移脚本不是幂等脚本，需生成或确认安全补迁移方案；
- 单位名称显示和HTTP请求键兼容修正已纳入功能分支本地提交；
- 测试环境实际Nginx仍需增加SPA History回退并重新部署；
- 商品名称即时查重、并发异常提示和外部同步同名规则仍待实现；
- 实际数据库 `uk_goods_name` 唯一索引仍待只读确认；
- OA、华数之家真实外部接口联调仍待授权；
- 生产数据库迁移尚未执行。
