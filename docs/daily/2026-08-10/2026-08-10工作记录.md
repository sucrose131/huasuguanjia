# 2026-08-10 工作记录

> 当前分支：`feature/20260809-requirements-delivery`
>
> 今日主题：新需求交付分支与 OA/外部系统分支整合规划

## 一、已完成

1. 核对 `dev`、`feature/sync`、`codex/0807-new-require` 和 `feature/20260809-requirements-delivery` 的提交关系；
2. 确认 `feature/sync` 是 `dev` 的直接后续，OA 分支本身可快进到 `dev`；
3. 确认 `feature/20260809-requirements-delivery` 完整包含当前新需求分支，并额外包含 7 个修复与交付提交；
4. 只读模拟合并并定位公共冲突文件；
5. 确定整合分支名为 `feature/20260810-requirements-sync`；
6. 新增[多分支整合计划](../../global/plans/多分支整合计划-20260810.md)。

## 二、当前结论

- 以 `feature/sync` 作为整合基线；
- 合并 `feature/20260809-requirements-delivery`，不再单独合并其祖先 `codex/0807-new-require`；
- OA 最新实现以 `feature/sync` 为准，新需求业务实现以 delivery 分支为准；
- 当前审批机制继续保留，本次整合不自动切换 OA；
- 冲突主要集中于环境配置、Git 忽略规则、依赖、Prisma schema、应用模块入口、锁文件和 TypeScript 构建缓存。

## 三、尚未执行

- 尚未创建整合分支；
- 尚未执行 Git merge；
- 尚未解决代码冲突；
- 尚未执行数据库迁移；
- 尚未形成整合提交或推送远程；
- 尚未进行合并后的自动化测试和浏览器验收。

## 四、Git 状态

- 本轮仅编写整合计划，没有执行分支切换或合并；
- 当前仍有未跟踪迁移文件 `database/migrations/20260809_complete_column_comments.sql`，正式整合前必须先保护并纳入明确提交；
- 今日整合计划尚未提交，Git 提交号：无。


## 五、整合执行结果

1. 远程 `dev` 已包含 `feature/sync` 的提交，整合分支从 `eee84e7` 创建；
2. 已把 `feature/20260809-requirements-delivery` 合入整合分支并解决 6 个冲突文件；
3. 环境示例同时保留 OA、华溯之家、十方清源、OSS 和售后回调配置；
4. AppModule 同时注册 IntegrationsModule 与 AttachmentsModule；
5. Prisma 保留 OA 模型、附件字段、新业务字段、`decimal(12,4)` 成本精度和缺料单号唯一约束；
6. pnpm 锁文件根据合并后的依赖重新生成；
7. 三个 tsbuildinfo 构建缓存已从版本控制移除并纳入忽略规则；
8. 真实外部集成测试改为显式 opt-in，默认测试不会创建 OA 流程或写外部同步数据。

## 六、验证结果

| 验证项 | 结果 |
| --- | --- |
| Prisma validate | 通过 |
| Prisma Client generate | 通过 |
| API/Web/Contracts typecheck | 全部通过 |
| API 安全自动化测试 | 153 项通过 |
| 真实外部集成测试 | 20 项默认跳过，待授权联调 |
| Web 自动化测试 | 5 项通过 |
| Contracts 测试 | 无测试文件，正常通过 |
| API/Web/Contracts build | 全部通过 |
| `pnpm dev` | 本机正常启动 |
| API health | status、MySQL、Redis 均为 up |
| Web HTTP | 200 |
| 冲突标记与 diff 格式 | 通过 |
| 浏览器视觉验收 | 当前浏览器控制不可用，待补 |
| lint | 存在来源分支已有规范欠账，未作为本次业务整合阻塞 |

## 七、数据库只读核对

- 通用出入库附件字段为 JSON 可空；
- 通用出入库明细成本为 `decimal(12,4)`，默认 `0.0000`；
- BOM 退库和售后进展字段注释已存在；
- 生产缺料附件字段已存在；
- 当前本地库没有 `uk_production_shortage_no`，但 4 条有效缺料记录均无重复；
- 本轮未执行任何数据库结构变更。

## 八、当前停点

- 整合分支：`feature/20260810-requirements-sync`；
- merge 冲突已全部解决，但尚未创建 merge commit；
- 尚未推送远程或合入 `dev`；
- `docs.zip` 保持为未跟踪用户文件，不纳入整合；
- 执行前 stash 仍保留，确认提交无误后再决定是否清理。
