# 采购前端恢复实施结果（审核点三）

> 日期：2026-08-20  
> 当前分支：`review/0820-remote-dev`  
> 范围：共享引擎骨架及采购、库存、领用、生产、销售/售后核心链路恢复与自审。

## 1. 本次已完成

1. 共享引擎新增页面新增权限和行操作权限过滤；查看使用页面权限，编辑使用 `update`，删除使用 `delete`，提交、审核、确认等业务动作使用各自独立权限；
2. 共享引擎新增完整详情加载、可配置弹框尺寸及页面/行操作/业务弹框/表单扩展插槽；
3. 采购申请恢复“整单生成 / 选品生成 / 关联订单”，并重新接入独立拆单弹框；
4. 采购拆单只允许选择尚未生成的明细，并继续叠加金额编辑白名单；
5. 采购订单明细改为输入总金额，只读展示“总金额 ÷ 采购数量”得到的计算单价；
6. 无金额查看权限时，总金额、计算单价及订单合计均显示 `****`；无金额编辑权限时不能保存；
7. 采购订单行恢复“付款”，点击后携带 `orderId` 进入付款登记；
8. 采购付款页面取消独立“登记付款”按钮，保留付款流水查询、查看和撤销管理。

## 2. 前端字段映射

| 页面字段 | API 字段 | 数据库映射 | 结论 |
| --- | --- | --- | --- |
| 整单/选品生成模式 | `generationMode` | 业务参数，不落独立字段 | 已映射 |
| 供应商 | `vendorId` | `hspsi_purchase_order.vendor_id` | 已映射 |
| 申请明细 | `applicationDetailId` | `hspsi_purchase_order_detail.source_application_detail_id` | 已映射，现有唯一索引防重复 |
| 明细总金额 | `totalAmount` | API 业务字段 | 已映射，不新增重复列 |
| 计算单价 | `unitPrice` | `hspsi_purchase_order_detail.unit_price` | 已映射 |
| 付款来源订单 | `orderId` | `hspsi_purchase_order_payment.po_id` | 已映射 |

本次没有新增表、增删字段或修改数据字典。

## 3. 已完成验证

- Web `vue-tsc --noEmit`：通过；
- `git diff --check`：通过；
- 本地浏览器采购申请列表：正常加载，无控制台错误；
- 本地浏览器采购订单新增弹框：已显示“总金额 / 计算单价”，无金额权限账号正确显示 `****`，保存按钮禁用；
- 本地浏览器采购付款页面：不再显示独立“登记付款”按钮，无控制台错误；
- 未提交任何采购单、付款或库存数据。

## 4. 后续自审完成项

1. 采购申请“整单生成 / 选品生成 / 关联订单”已恢复，并取消错误的金额白名单入口隐藏；
2. 采购服务拆单事务锁、防重复、删除释放映射及金额计算规则已完成代码审计；
3. 库存盘点恢复同一弹框“创建并载入明细 → 填写实盘数量 → 保存并提交”的连续操作；
4. 领用申请的所属组织已在前后端强制固定为登录人的 OA 归属组织；
5. 生产出库恢复独立的补料明细弹框，真实历史补料单已在浏览器验证；
6. 全链路追溯和单据附件已恢复为共享单据能力，采购操作记录通过独立采购页面壳恢复；
7. 详情接口缺少标准 `id` 时由共享引擎保留列表单据身份，采购订单详情接口同时补齐标准 `id`。
8. 权限回归发现并修复模块目录可代替页面查看权限的越权问题；系统管理接口改为角色、用户、菜单各自的精确页面权限；
9. 本地 `hspsi-dev` 执行已有幂等补丁 `20260818_per_menu_actions_and_manual_org_scope.sql`，业务动作权限由 4 条补齐为 209 条；基础申请人恢复采购/领用申请的新增、编辑、删除和提交权限；
10. OA 用户固定主组织授权完成校验：缺少主组织授权 0 人，错误自动跨组织授权 0 条；人工授权和金额白名单未被迁移修改；
11. 采购申请、生产计划、库存调整、库存调拨的驳回操作恢复必填真实驳回原因。
12. 修复重构后所有业务按钮默认复用 `update` 权限的问题；付款、收退款、出入库、退货、售后及跨页面办理入口均改为目标菜单/目标动作的精确权限，避免“有编辑权即可执行其他业务动作”。

## 5. 回归结果

- 采购服务测试：26 项通过；
- 库存服务测试：45 项通过；
- 领用服务测试：22 项通过；
- 生产服务测试：15 项通过；
- 销售/售后服务测试：38 项通过；
- Web 类型检查与 `git diff --check`：通过；
- API、Web 生产构建：通过；本轮合并回归 141 项通过，分阶段累计覆盖 146 项业务测试；
- 浏览器验证：采购、库存盘点、生产出库页面正常，无业务控制台错误；金额掩码、采购付款入口、附件、追溯、操作记录、补料明细入口符合规则；
- 真实接口回归：采购申请选品生成临时订单后删除，申请明细状态由 `generated` 正确释放为 `not_generated`；盘点草稿创建后在同一会话读取 3 条库存明细并删除；跨组织领用申请返回 403，本组织草稿正确固定为组织 9 并删除；
- 普通申请人：系统用户/角色接口 403，未授权库存和采购订单写入 403，采购申请新增进入业务字段校验；金额返回 `null`；金额白名单账号返回真实金额并进入金额业务校验；
- 所有临时采购订单、盘点单和领用申请草稿均已删除，未遗留本轮测试单据。
- 精确权限测试 11 项通过，API 全量测试中 408 项通过；3 个真实华溯之家集成测试仅因当前测试终端缺少外部接口环境变量未执行成功，与本次业务代码无关；
- 本地浏览器复验采购订单“更多”菜单：仅展示当前账号同时具备权限且符合单据状态的“生成入库、退回未到货、操作记录、全链路追溯”，金额白名单继续独立生效。

补充：API 的开发态全量 `tsc -p tsconfig.json` 仍会扫描已有第三方集成测试文件，存在华溯之家、十方清源测试类型错误；正式构建使用的 `tsconfig.build.json` 已通过，本次业务改造未新增此类错误。

## 6. 提交后结构收口

1. 将共享单据页面的权限推导抽取到 `use-business-document-permissions.ts`，统一页面、新增及行操作权限；
2. 将字典、按页面配置加载的选项集合和组织联动抽取到 `use-business-document-options.ts`，未恢复无条件全量加载；
3. 将追溯弹框状态和展示抽取到 `BusinessDocumentTrace.vue`；
4. 采购申请三个生成入口改为精确校验 `purchase:applications:generate-order`，不再借用 `update` 权限；
5. `BusinessDocumentPage.vue` 从 574 行收口至 463 行，只保留列表、查询、表单和扩展插槽编排；
6. Web 类型检查、生产构建及 `git diff --check` 通过；浏览器复验采购申请的审批、操作记录和全链路追溯入口正常。
