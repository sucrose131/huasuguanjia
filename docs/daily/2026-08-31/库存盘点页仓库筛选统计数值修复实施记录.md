# 库存盘点页仓库筛选统计数值修复实施记录

> 日期：2026-08-31
> 分支：未建分支（当前工作区直接修改）
> 类型：影响用户操作的界面缺陷修复（业务人员报障，范围已确认）
> 当前状态：代码完成，前后端类型检查、单元测试与浏览器验证通过（未提交、未推送）

## 一、问题描述

库存盘点页面（`/inventory/checks`）仓库筛选组件统计数值不正确：

1. 各仓库 Tab 显示的是「有库存品项数」（来自通用接口 `/inventory/warehouses/tabs`），而页面列表是盘点单——两者单位不一致，例如「全部 19」但某仓库 Tab 显示「22」，数值明显错乱；
2. 「全部」项统计绑定当前查询总数，点击某仓库后随之变为该仓库的数量（与库存查询页同类问题）。

## 二、根因分析

- 盘点页使用共享组件 `WarehouseTabs`，但未传入 `warehouseCounts`，组件回退到 `/inventory/warehouses/tabs` 的 `item.count`（有库存品项数），与盘点单列表口径不符；
- 对比库存预警/效期预警页面：其后端接口返回各仓库单据数 `warehouseCounts` 供 Tab 展示；盘点接口 `/inventory/checks` 未返回该字段；
- 「全部」显示当前筛选 `total`，随仓库点击变化（库存查询页同类接线问题）。

## 三、修改内容

| 文件 | 位置 | 修改 |
| --- | --- | --- |
| `apps/api/src/inventory/inventory.service.ts` | `checks()` | 新增按 `warehouse_id` 分组的盘点单数统计 `warehouseCounts`（按组织统计、忽略已选仓库、排除 `warehouse_id=0`），随响应返回 |
| `apps/web/src/views/business/InventoryCheckPage.vue` | `query-tools` 插槽 | 将 `warehouseCounts` 传入 `WarehouseTabs`；「全部」统计改为各仓库盘点单数之和（`orgWarehouseCheckTotal`），不再随选中仓库变化 |

## 四、影响范围

- 仅影响库存盘点页仓库筛选的统计展示；列表、汇总卡、筛选与其它页面不受影响；
- 接口 `/inventory/checks` 新增返回字段 `warehouseCounts`（向后兼容，其它调用方无影响）；
- 库存预警、效期预警页面共用 `WarehouseTabs` 组件但数据流不变，未受影响。

## 五、验证结果

- Web TypeScript 类型检查：通过；Web 单元测试：17 个文件 59 项全部通过；
- API TypeScript 类型检查：本次改动无新增报错（`user-sync.unit.spec.ts` 两处为既有问题，与本次无关）；
- 浏览器验证（Playwright，本地开发服务）：
  - 修复前：全部 19，仓库 Tab 显示有库存品项数（如「销售-实体成品仓 22」）；
  - 修复后：全部 19（= 各仓库盘点单数之和），仓库 Tab 显示盘点单数（如「销售-实体成品仓 6」）；
  - 点击仓库后「全部」保持 19 不变，列表正确筛选为该仓库（共 6 条）。

## 六、遗留问题与风险

- 库存预警、效期预警页面的「全部」统计同样绑定当前筛选总数（点击仓库会变化），与本次修复前盘点页行为一致；业务确认后可按同样方式处理。

## 七、补充修复：仓库 Tab 统计回退显示库存数

业务复查发现：华溯控股（深圳）有限公司（org 9）的「健服-医疗耗材仓」计数仍不对（显示 2，点击后列表为空）。

### 根因

`WarehouseTabs.vue` 各仓库 Tab 的取值逻辑为 `warehouseCounts?.[warehouseId] ?? item.count ?? 0`：当某仓库盘点单数为 0（`warehouseCounts` 无该键）时，`??` 回退到 `/inventory/warehouses/tabs` 的 `item.count`（有库存品项数）。健服-医疗耗材仓实际盘点单数为 0，但有 2 个有库存品项，故显示 2，与点击后的空列表不一致。

### 修改

| 文件 | 位置 | 修改 |
| --- | --- | --- |
| `apps/web/src/components/business/WarehouseTabs.vue` | 仓库 Tab 统计取值 | 提供 `warehouseCounts` 时以它为准，缺失即显示 0，不再回退 `item.count`；未提供时才回退 `item.count` |

### 验证

- 浏览器验证（未选组织视角）：健服-医疗耗材仓由「2」改为「0」，点击后列表「共 0 条」，Tab 与列表一致；
- API 核对（org 9）：`warehouseCounts = {仓库1: 6, 仓库15: 4, 仓库2: 1}`，健服-医疗耗材仓（仓库 9）盘点单数为 0，与页面一致；
- Web 类型检查与 59 项单元测试通过。

### 数据观察（非代码问题）

- org 9 有 4 张盘点单挂在「华溯全链路原料仓」（仓库 15，`status=2` 停用），导致「全部」与各仓库 Tab 之和对不上；业务确认停用仓库单据不应计入统计。

## 八、补充修复：停用仓库单据不计入「全部」统计

业务复查确认：停用仓库既然是停用的，其盘点单不应统计到「全部」及各仓库统计中（曾尝试把停用仓库补为 Tab，方向不对，已放弃该方案）。

### 根因

`checks()` 的列表与 `warehouseCounts` 统计都包含停用仓库（如仓库 15「华溯全链路原料仓」status=2）的单据，而仓库 Tab 只列启用仓库，导致「全部」与可见 Tab 之和、列表条数不一致。

### 修改

| 文件 | 位置 | 修改 |
| --- | --- | --- |
| `apps/api/src/inventory/inventory.service.ts` | `checks()` | 先查当前组织范围内启用仓库（`status=1`、未删除）；列表未选仓库时仅返回启用仓库的单据，`warehouseCounts` 同样只统计启用仓库单据 |
| `apps/api/src/inventory/inventory.service.ts` | `checks()` | 移除上一版 `warehouseTabs` 补充字段 |
| `apps/web/src/components/business/WarehouseTabs.vue` | 组件 | 移除 `extraTabs` 属性与合并逻辑（还原） |
| `apps/web/src/views/business/BusinessDocumentPage.vue` | 共享引擎 | 移除 `warehouse-tabs` 插槽透出（还原） |
| `apps/web/src/views/business/InventoryCheckPage.vue` | `query-tools` 插槽 | 移除 `:extra-tabs` 传参（还原） |

### 验证

- API 核对（admin 全量）：`total = 15`，`warehouseCounts = {仓库1:6, 仓库2:1, 仓库12:1, 仓库16:1, 仓库51:1, 仓库87:5}`，之和 15；不再包含仓库 15 的 4 张单；
- API 核对（org 9）：`total = 7`，`warehouseCounts = {仓库1:6, 仓库2:1}`，之和 7（停用仓库 4 张不计入）；
- 浏览器验证：盘点页「全部 15」，各仓库 Tab 之和 = 15，列表「共 15 条」，三者一致；无停用仓库 Tab；
- Web 类型检查与 59 项单元测试通过；API 类型检查无新增报错。

## 九、Git 提交

- 未提交（按业务负责人要求，先实现不提交）。
