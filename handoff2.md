# 全系统组织、仓库、商品联动约束改造 Handoff 2

> 交接日期：2026-08-13
>
> 工作分支：`vanny/20260812-new-task`
>
> 远端仓库：`origin`（公司 Gitea）

## 一、本次交付目标

统一全系统业务单据中的组织、仓库、商品分类和 SKU 约束：

- 仓库必须启用并属于单据所选组织；
- 商品必须启用，且属于单据组织或为全局商品；
- 商品分类必须启用，并与仓库类型一致；
- SKU 必须启用且属于所选商品；
- 前端负责联动筛选和清理，后端在草稿保存和库存过账阶段执行强校验；
- 商品范围使用单据 `orgId + warehouseId`，不使用登录账号组织替代单据组织。

本次未修改数据库结构、数据字典或历史业务数据。

## 二、已完成实现

### 1. 公共主数据约束

- 新增 `BusinessMasterDataService`，集中提供组织—仓库、商品—分类—仓库类型及商品—SKU 校验；
- 公共服务由数据库模块导出，供库存、生产、销售和领用模块复用；
- 库存过账继续作为最终落账防线。

### 2. 生产、销售、领用和库存

- 生产 BOM、生产计划、生产出库、成品入库、临时出库接入统一校验；
- 销售订单、折价销售、销售退货草稿接入统一校验；
- 领用申请、直接领用出库、领用退回草稿接入统一校验；
- 库存调拨、盘点、报损及通用出入库补齐保存阶段校验；
- 相关前端表单按单据组织过滤仓库，按仓库类型过滤商品，切换组织或仓库时清理失效明细。

### 3. 售后记录专项补强

- 售后主记录关联销售订单，一条售后支持追加多条客服处理进展；
- 进展新增、修改和软删除均保留操作日志；
- 处理人由后端强制取当前登录用户，不能信任请求体中的处理人 ID；
- 外部同步或系统生成的进展禁止人工编辑、删除；
- 前端时间线显示“人工记录／外部同步／系统记录”来源。

华溯之家真实售后接口联调仍按业务决定暂缓。

### 4. Prisma 同步

- Prisma 模型已按项目实际连接的 `hspsi-dev-ai` 数据库选择性同步并重新生成 Client；
- 未执行 Prisma migration，未修改实际数据库；
- 实际数据库尚无 `hspsi_sale_order_source_mapping` 表，但既有集成代码依赖对应模型，因此当前仍保留该 Prisma 模型。

## 三、验证结果

- API TypeScript 类型检查通过；
- Web TypeScript 类型检查通过；
- 核心组织—仓库—商品约束测试：97 项通过；
- 售后模块专项测试：21 项通过；
- API 全量测试此前结果：182 项通过、29 项外部集成测试跳过；
- 华溯之家真实请求测试因缺少外部连接环境变量未运行；
- 浏览器取消式验收覆盖 BOM、销售订单、领用申请、生产成品入库及售后记录，未保存测试业务单据；
- `git diff --check` 已通过后方可提交。

## 四、历史数据只读审计

审计脚本：`apps/api/src/scripts/audit-business-master-links.ts`

未发现异常的范围：

- 启用仓库和商品分类的仓库类型配置；
- 销售订单组织—仓库—商品关系；
- 生产 BOM 组织—仓库—原料关系；
- 库存调拨来源及目标组织—仓库关系。

发现 3 条领用申请历史异常，未做任何数据修复：

1. `DR202607311900850003`：商品分类类型与仓库类型不一致；
2. `DR20260811000001`：商品组织为 3，单据组织为 9；
3. `DR20260811000003`：商品组织为 3，单据组织为 9。

必须由业务负责人确认真实组织、仓库和商品归属后另行处理。

## 五、采购申请按供应商拆单的新需求结论

业务已确认：采购申请审批通过时，将全部申请明细按照商品主档供应商分组，每个供应商一次性生成一张采购订单；入库、付款、退货等后续链路不变。

当前结论：

- 该需求尚未编码，不能视为本次已交付功能；
- 在“一次性拆完、不做部分转单、不做二次转单”的范围内，无需修改数据库表结构；
- 无需修改数据字典；
- 后端需将当前“固定生成一张 `vendor_id=0` 订单”改为按 `hspsi_goods_info.vendor_id` 分组生成多张订单；
- 同一事务内任何一组生成失败应全部回滚；
- 商品缺少有效供应商时必须阻止审批，并明确列出异常物料；
- 前端采购申请明细应展示供应商，审批前展示拆单预览，审批后展示关联采购订单列表；
- 如果未来增加部分转单、同一明细分批或拆给多个供应商，再增加采购订单明细来源申请明细字段。

## 六、相关文档

- `docs/global/plans/全系统组织仓库商品联动约束改造Handoff-20260812.md`
- `docs/daily/2026-08-12/全系统组织仓库商品联动约束改造实施记录.md`
- `docs/daily/2026-08-12/全系统组织仓库商品联动历史数据审计.md`

## 七、接手注意事项

- 不得把当前约束改造扩展为账号或角色数据权限；
- 不得只保留前端过滤，后端保存阶段强校验不能移除；
- 不得未经业务确认新增数据库字段、字典或修复历史数据；
- `.codex-tmp/`、`docs/global/.DS_Store` 属于本地临时或系统文件，不应提交；
- 采购按供应商拆单属于下一项已确认设计、尚未实现的需求。
