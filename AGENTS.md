# 项目开发约束

本项目所有新增功能、业务改造和影响用户操作或业务数据的缺陷修复，必须遵守：

- `docs/global/rules/AI功能开发与交付规则.md`

核心顺序不得颠倒：

```text
需求确认 → 数据库核对 → 界面设计  → 字段/API/数据库映射 → 业务确认
→ 前后端实现 → 自动化与浏览器验证 → 同步实施构想和实施记录表
```

未经业务负责人确认，不得擅自新增表、字段或数据字典，不得用仅前端过滤代替后端权限控制，不得在主业务表单成功保存前把快捷新增的临时资料写入数据库。

实施记录以原始一级需求为固定目录，子项拆分到下一位开发人员能够理解、验证和继续接手的粒度即可，不拆成无意义的按钮或代码流水。
